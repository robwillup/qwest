using Quest.Commands;
using Quest.Commands.ToDo;
using System.IO;
using Xunit;

namespace QuestTests.CommandsTests.DontTests
{
    public class DontHandlerTests
    {
        //[Fact]
        //public void DeleteToDo_PassIfReturnsTrue()
        //{
        //    // Arrange            
        //    string todo = "this is a test";
        //    string filePath = Path.Combine(Directory.GetCurrentDirectory(), "test.md");
        //    string hashCode = DoRoutines.Create(filePath, todo).ToString();

        //    // Act
        //    DontHandler.DeleteTodo(hashCode, filePath);
        //    string[] content = ToDoHandler.ListToDos(filePath);
        //    // Assert
        //    Assert.DoesNotContain(hashCode, content);
        //}
    }
}
